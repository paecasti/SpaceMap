<UserControl x:Class="SpaceMap.App.Views.DirectoryBreakdownView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <DockPanel Margin="0,0,0,8">
            <TextBlock Text="Minimum filter (bytes):" VerticalAlignment="Center" />
            <TextBox Width="120"
                     Margin="8,0,0,0"
                     Text="{Binding DirectoryBreakdown.MinimumSizeBytes, UpdateSourceTrigger=PropertyChanged}" />
        </DockPanel>
        <DataGrid AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  ItemsSource="{Binding DirectoryBreakdown.Items}"
                  SelectedItem="{Binding Results.SelectedItem, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Binding="{Binding Path}" Header="Path" />
                <DataGridTextColumn Width="120" Binding="{Binding Kind}" Header="Type" />
                <DataGridTextColumn Width="160" Binding="{Binding RealSizeBytes, StringFormat={}{0:N0}}" Header="Real bytes" />
                <DataGridTextColumn Width="160" Binding="{Binding LogicalSizeBytes, StringFormat={}{0:N0}}" Header="Logical bytes" />
                <DataGridCheckBoxColumn Width="90" Binding="{Binding Partial}" Header="Partial" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
